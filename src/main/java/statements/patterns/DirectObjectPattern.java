package statements.patterns;

/**
 * A builder for a proxy DirectObject.
 */
public class DirectObjectPattern extends ComponentPattern {
    public DirectObjectPattern() {
        super(statements.core.DirectObject.class);
    }
}
