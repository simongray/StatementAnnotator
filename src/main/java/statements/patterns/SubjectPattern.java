package statements.patterns;

/**
 * A builder for a proxy Subject.
 */
public class SubjectPattern extends ComponentPattern {
    public SubjectPattern() {
        super(statements.core.Subject.class);
    }
}
